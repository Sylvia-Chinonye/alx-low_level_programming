#include <stdio.h>
/**
 * main - prints name of the file that its compiled from
 * Description: a header file
 *
 * Return: always 0
 */
int main(void)
{
	printf("Filename: %s\n", _FILE_);
	return (0);
}
